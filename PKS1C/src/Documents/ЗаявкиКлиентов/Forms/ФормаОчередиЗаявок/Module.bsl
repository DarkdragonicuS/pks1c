
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("ОчередьЗаявок") Тогда
		Для Каждого Заявка Из Параметры.ОчередьЗаявок Цикл
			СтрокаОчереди = ОчередьЗаявок.Добавить();
			СтрокаОчереди.Заявка = Заявка;
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПродолжитьРаботу(Команда)
	Закрыть(Новый Структура("Причина",Причина));
КонецПроцедуры

#КонецОбласти