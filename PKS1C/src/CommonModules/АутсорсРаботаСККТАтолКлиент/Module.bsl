//Возвращает дату 1С из строки, полученной из JSON-строки ККТ
//Формат даты ККТ: "ГГГГ-ММ-ДДTЧЧ:мм:ссЧасовойПояс"  	
//Пример: "2022-08-23T15:30:00+11:00"
Функция ПолучитьДатуИзСтрокиJSON(ДатаСтрока) Экспорт
	Год = Лев(ДатаСтрока,4);
	Месяц = Сред(ДатаСтрока,6,2);
	День = Сред(ДатаСтрока,9,2);
	Час = Сред(ДатаСтрока,12,2);
	Минута = Сред(ДатаСтрока,15,2);
	Секунда = Сред(ДатаСтрока,18,2);
	
	ДатаРезультат = Дата(Год+Месяц+День+Час+Минута+Секунда);
	
	Возврат ДатаРезультат
КонецФункции